# PyTorch: install from PyTorch index for CUDA 12.1, then run pip install -r requirements.txt
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
setuptools>=65.0,<82  # pkg_resources (removed in 82) required by tensorboard
torch>=2.3.0
torchvision>=0.18.0
numpy<2  # Required for PyTorch 2.3.0 compatibility
Pillow>=9.0
matplotlib>=3.5.0
tensorboard>=2.10
tqdm>=4.65.0
albumentations>=1.3.0
huggingface_hub>=0.20.0
shapely>=2.0 
ipywidgets>=8.0
mkdocs>=1.5.0
mkdocs-material>=9.0
mkdocstrings[python]>=0.23.0
gradio==6.8.0
